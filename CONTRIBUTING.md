# Contributing

Thank you for helping repository facts stay trustworthy.

1. Create a focused branch.
2. Add or update tests for every rule change.
3. Run `npm run format`, `npm run lint`, `npm test`, and `npm run package`.
4. Explain the false-positive tradeoff in the pull request.

New rules should prefer strong evidence and silence over broad keyword matching.
